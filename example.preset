#
# example preset file for geninit
#
# - presets can be disabled by prefixing with !
# - each preset must have, at minimum, an image name defined
# - extra options and config parameters are optional
# - kver can be specified as a path to a kernel image or a version
#

presets=(default failsafe)
ALL_kver=/boot/vmlinuz26
ALL_config=/etc/geninit.conf

default_image=/boot/initramfs-ARCH

failsafe_image=/boot/initramfs-ARCH-failsafe
failsafe_options="-S autodetect"
failsafe_config=/etc/geninit.conf
